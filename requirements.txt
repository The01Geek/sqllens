# SQL Lens — resolved runtime lockfile (S-12).
#
# Exact, fully-resolved transitive closure of the *core* runtime
# dependencies declared in pyproject.toml's [project.dependencies]
# (none of the optional [dev] / [postgres] / [mysql] / [all] extras
# are installed; note `uvicorn[standard]` IS a core dep, so its
# extra's deps — uvloop, watchfiles, httptools, websockets,
# python-dotenv — legitimately appear in this closure). Every pin here
# satisfies the conservative upper bounds in pyproject.toml — those
# ranges remain the source of truth and MUST NOT be loosened. This
# file records ONE known-good resolution shipped with the wheel for
# inspection/provenance. It is a point-in-time snapshot, not a
# continuously-verified guarantee — nothing in CI re-resolves it
# (a drift guard is tracked separately; see the regen procedure).
#
# Regenerate (clean resolution, no extras):
#
#   python -m venv /tmp/lockenv
#   /tmp/lockenv/bin/pip install --upgrade pip
#   /tmp/lockenv/bin/pip install .          # relies on pip's default
#                                           # build isolation (do NOT
#                                           # pass --no-build-isolation,
#                                           # or backend tools leak in)
#   /tmp/lockenv/bin/pip freeze | grep -ivE '^sqllens([= @]|$)' > requirements.txt
#   # then re-add this header
#
# (`pip install .` is non-editable, so `sqllens` itself appears in the
# freeze output — the grep drops it, matching `sqllens==` and any
# `sqllens @ file://...`/`sqllens` form. `build` / `pyproject-hooks`
# below are NOT build-backend leakage: chromadb declares `build>=1.0.3`
# as a real runtime dependency, and `build` in turn requires
# `pyproject-hooks` — both are legitimately in the runtime closure.)
#
# Approximately equivalent via pip-tools — resolves the same
# [project.dependencies] closure (including chromadb's `build`), though
# resolver/marker handling can differ and the output is not byte-identical:
#
#   pip install pip-tools
#   pip-compile --strip-extras --output-file requirements.txt pyproject.toml
#
# Keep alphabetically sorted. Bump only via the procedure above so the
# closure stays internally consistent; never hand-edit a single pin.
#
# Dependabot's weekly grouped `pip` PR (see .github/dependabot.yml)
# WILL propose single-pin bumps to this file. Treat that PR as a drift
# signal only: do not merge its piecemeal pin edits — instead rerun the
# procedure above so the whole closure is re-resolved together, then
# update the PR (or close it in favour of the regenerated commit).

annotated-doc==0.0.4
annotated-types==0.7.0
anthropic==0.102.0
anyio==4.13.0
attrs==26.1.0
bcrypt==5.0.0
build==1.5.0
certifi==2026.4.22
cffi==2.0.0
charset-normalizer==3.4.7
chromadb==1.5.9
click==8.4.0
cryptography==48.0.0
distro==1.9.0
docstring-parser==0.18.0
durationpy==0.10
filelock==3.29.0
flatbuffers==25.12.19
fsspec==2026.4.0
googleapis-common-protos==1.75.0
greenlet==3.5.0
grpcio==1.80.0
h11==0.16.0
hf-xet==1.5.0
httpcore==1.0.9
httptools==0.7.1
httpx==0.28.1
httpx-sse==0.4.3
huggingface-hub==1.15.0
idna==3.15
importlib-metadata==8.7.1
importlib-resources==7.1.0
jiter==0.14.0
jsonschema==4.26.0
jsonschema-specifications==2025.9.1
kubernetes==35.0.0
markdown-it-py==4.2.0
mcp==1.27.1
mdurl==0.1.2
mmh3==5.2.1
numpy==2.4.6
oauthlib==3.3.1
onnxruntime==1.26.0
opentelemetry-api==1.41.1
opentelemetry-exporter-otlp-proto-common==1.41.1
opentelemetry-exporter-otlp-proto-grpc==1.41.1
opentelemetry-proto==1.41.1
opentelemetry-sdk==1.41.1
opentelemetry-semantic-conventions==0.62b1
orjson==3.11.9
overrides==7.7.0
packaging==26.2
pandas==3.0.3
protobuf==6.33.6
pybase64==1.4.3
pycparser==3.0
pydantic==2.13.4
pydantic-core==2.46.4
pydantic-settings==2.14.1
pygments==2.20.0
pyjwt==2.12.1
pypika==0.51.1
pyproject-hooks==1.2.0
python-dateutil==2.9.0.post0
python-dotenv==1.2.2
python-multipart==0.0.29
pyyaml==6.0.3
referencing==0.37.0
requests==2.34.2
requests-oauthlib==2.0.0
rich==15.0.0
rpds-py==0.30.0
shellingham==1.5.4
six==1.17.0
sniffio==1.3.1
sqlalchemy==2.0.49
sqlglot==25.34.1
sse-starlette==3.4.4
starlette==1.0.0
tenacity==9.1.4
tokenizers==0.23.1
tqdm==4.67.3
typer==0.25.1
typing-extensions==4.15.0
typing-inspection==0.4.2
urllib3==2.7.0
uvicorn==0.47.0
uvloop==0.22.1
watchfiles==1.2.0
websocket-client==1.9.0
websockets==16.0
zipp==4.1.0
