# Normalize line endings to LF for all text files, regardless of the
# platform or partition the repo is copied from. Prevents spurious
# whole-tree CRLF diffs (e.g. after copying from a Windows partition).
* text=auto eol=lf
